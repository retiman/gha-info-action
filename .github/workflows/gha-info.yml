name: Show GHA info

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  info:
    runs-on:
      - ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Show
        uses: ./
