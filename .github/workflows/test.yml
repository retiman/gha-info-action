name: Test

on:
  pull_request:

jobs:
  print:
    runs-on:
      - ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: test
        uses: ./.github/actions/what
