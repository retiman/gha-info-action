name: Test

on:
  pull_request:

jobs:
  print:
    runs-on:
      - ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Say what?
        uses: ./.github/actions/what
